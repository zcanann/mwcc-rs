// (float) of a double rounds to single with frsp; a leaf rounds in place, a
// sub-expression is computed into the destination first.
float tofs(double x){ return (float)x; }
float sumf(double a, double b){ return (float)(a + b); }
